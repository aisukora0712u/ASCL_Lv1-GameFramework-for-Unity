# 长期路线图与实施追踪验收记录

验收日期：2026-09-16（Asia/Tokyo）。本记录是本次交付的验证证据快照，后续进度以目标文件为准。

## 交付与范围

- 根目录 PLAN.md、GOALS.md，7 份阶段计划，97 份独立目标文件。
- 34 项已确认约束、16 个能力领域、136 个能力项；每项能力都关联至少一个目标。跨阶段能力共享同一能力 ID。
- 49 个阶段实施目标（含 6 个阶段退出验收）、39 个项目驱动候选、8 个暂缓专项、1 个文档与工具准备项。
- 96 个未来框架目标均为待安排；纳入阶段实施的完成率为 0/49。仅 ASCL-DOC-001 验收完成。
- S6 首次正式安排目标时须新增阶段退出验收；扩大范围时更新并重新安排验收。正式预测回滚在 S3，未归入暂缓专项。
- 仓库 Markdown 为唯一权威来源。新增只读 API、CLI、首页入口和 #implementation 专页，现有研究型扩展路线图保留。
- 本轮没有修改 Runtime、DevProject 或 .NET 框架实现，没有开始 S0–S6 功能实施。

## 自动验证

环境：Windows、Node.js 24、仓库现有依赖。没有新增或升级依赖。

| 检查 | 结果与覆盖 |
| --- | --- |
| 路线图校验 | 7 阶段、16 领域、136 能力、97 目标；无诊断 |
| 全部工作台测试 | 45 项通过：原有 33 项 + 路线图 12 项 |
| TypeScript | tsc --noEmit 通过 |
| 生产构建 | Vite 构建通过；既有依赖的 PURE 注释警告不影响产物 |

路线图测试覆盖：真实文档和 CLI；状态分母及零分母；只读与刷新版本；完成证据和未勾选验收；依赖就绪；重复 ID、未知引用、循环；阶段和能力覆盖；退出验收依赖；阶段调整时稳定 ID；阻塞/暂缓/取消原因；重复 YAML 键；目录重解析点；HTTP 成功、错误诊断、拒绝写入及来源/私人库字节不变。

原有测试覆盖存储事务、真实子进程强制退出恢复、锁、来源记录、CLI、备份、路径和 HTTP 边界。本轮未宣称 Unity/.NET 游戏逻辑、移动端或 IL2CPP 的功能验收。

## 实际浏览器验收

使用 Codex 内置浏览器访问本机生产地址 http://127.0.0.1:4317/#implementation。

- 总览准确显示 0/49；7 张阶段卡显示完成数及进行中、待验收、阻塞数，S6 候选数量独立展示。
- 阶段页显示完整计划、实际依赖、交付物、退出条件和目标目录。
- 目标清单按 S3、D14 与“重连”组合筛选得到对应目标；搜索后可打开完整正文及依赖目标。
- ASCL-S3-006、ASCL-S3-004 详情与依赖深链接可用，未来目标保持待安排、无虚构实施证据。
- 覆盖矩阵列出 16 领域、136 能力；展开联机领域可追踪至 S0/S3 等承接目标。
- 在隔离文档副本中加入不存在的依赖，页面准确显示文件及错误，抑制完成率；恢复副本后刷新，诊断消失，显示有效汇总。正式目标文件未用于错误注入。
- 已检查实际桌面截图，布局、阶段卡和详情内容可读。
- 最终生产版本复核：首页入口显示 0/49 并进入专页；准备项目显示已完成和全部验收证据，实施动态仅含本次实际日志。完整计划内的阶段/目标链接有效，其他仓库文档保留为明确的文件引用，外部参考链接保留原地址。

自动刷新机制：初次打开读取；页面可见时每 30 秒读取；重新获得焦点、恢复可见及手动刷新时读取。解析失败不给出可信百分比；请求失败时保留内容会明确标为旧数据。

## 数据边界

路线图读取器只读取 PLAN.md、GOALS.md 和 Docs/Plans、Docs/Goals 的直接 Markdown 子文件；CLI 不初始化私人库。GET 没有写回路径，目标未复制到私人知识库，原数据及备份结构未改变。

隔离测试比较 GET 前后来源和私人库的文件字节，结果不变。正式知识文章没有因本功能修改。工作台原有启动流程会执行来源检查并记录检查历史，因此重启前后的整个私人库摘要可能变化；这些既有检查记录不属于路线图写入。

## 可重复命令

从 Tools/KnowledgeSite 执行：

```powershell
pnpm roadmap:validate
pnpm roadmap:summary
pnpm test
pnpm build
```

已有 Node 环境也可直接执行：

```powershell
node server/roadmap-cli.ts validate
node --test --test-concurrency=1 tests/*.test.ts
node node_modules/typescript/bin/tsc --noEmit
node node_modules/vite/bin/vite.js build
```

工作台按原有启动方式运行。页面状态只从目标文件读取；后续实施须按照 GOALS.md 和 AGENTS.md 更新对应目标、验证与证据，并与实现同批提交。
