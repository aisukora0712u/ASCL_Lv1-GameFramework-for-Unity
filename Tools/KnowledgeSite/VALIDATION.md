# 首版验证记录

验证日期：2026-09-15（Asia/Tokyo）。环境：Windows，Node.js 24.19.0，pnpm 11.19.0，Vite 7.3.6；前端使用生产构建，浏览器操作使用 Codex 应用内 Chromium。下述结果只属于本网站，不表示重新运行过 ASCL / Unity 的历史测试。

## 自动验证

`node --test --test-concurrency=1 tests/*.test.ts`：**33 项通过，0 失败、0 跳过**。`tsc --noEmit` 与生产构建通过。所有异常、崩溃和恢复场景使用隔离临时目录。

| 范围 | 实际覆盖 |
| --- | --- |
| 记录与关系 | 五类新建、编辑；未知扩展字段和未编辑正文保留；全文件摘要；重命名保留身份；重复 ID 与失效关系诊断；一般循环允许、依赖循环拒绝 |
| 并发与历史 | 两写入者竞争、CLI 与网页共用基线、保存与删除竞争；软删除、入站引用保留；恢复生成新版本；重复操作和响应丢失不重复历史 |
| 真实进程终止 | snapshots、prepared、before-replace、temporary、replaced-0、before-commit、response 七阶段终止子进程；附件双文件事务分别在第一次和第二次替换后终止 |
| 写入失败 | 注入 ENOSPC / EPERM；Windows 实际文件共享锁阻止重命名时保留原文件；第三种磁盘内容保留并阻止自动覆盖 |
| 检索 | 中文单字、连续词组、完整 API、代码和表格、来源与附件说明；软删除筛选；扫描更新结果与反向引用 |
| 证据与复核 | 文件修改、目录内新增/删除、相关配置、Git 范围内未提交修改、范围外修改不误报；未绑定状态；自动检查不刷新实质复核；文章修改使旧复核失效 |
| 备份迁移 | 保留错误原文、普通未识别文件、历史与旧附件；备份期间外部修改拒绝导出；压缩期间释放全部写锁；损坏包、越界、未知格式拒绝；切换前中断保留旧库，切换后响应丢失可按原操作确认；旧世代写入拒绝 |
| 本地保护 | 精确 Host、Origin、实例令牌；路径边界、Windows 名称、大小写与目录联接；文件实际签名、像素限额；活跃或未知锁不按超时抢占 |

`node tests/runtime-check.ts`：**5 组通过**。该命令要求 4317 端口空闲，遇到已有服务即停止，不会杀进程。覆盖其他程序占用端口、陈旧实例记录、中文与空格目录、重复启动、错误库/实例身份、启动命令退出后后台服务继续运行及正常停止。首次 `Prepare.ps1` 和日常 `Start.ps1 -NoBrowser` / `Stop.ps1` 均实际执行。

## 浏览器操作

在合成验收库中实际完成：

- 五类记录的新建、编辑与关联，固定双方版本的对比矩阵。
- Markdown 表格、代码、Mermaid SVG、位图及 PDF.js 画布预览；原始 HTML 不执行，远端图片显示禁止加载提示。
- 两标签页同基线保存冲突，保留双方内容并查看差异；合并后保存。
- 草稿关闭、刷新、恢复；B 标签页保存后 A 标签页草稿仍完整可恢复。
- 站内确认框的确认、取消与 Escape；实质复核表单；历史恢复后旧复核失效。
- 移入回收区、删除前历史恢复；回收区列表、筛选数量一致。
- 人工来源版本登记、文件选择器上传合成 PNG、完整备份下载、暂停与恢复受管写入。
- 含标点 API + 模块 + 状态组合检索；WebMCP 搜索、打开、新建表单及无效 ID 拒绝。
- 桌面与 390 × 844 窄屏、明暗主题、侧栏滚动、键盘操作；窄屏正文未产生横向页面溢出。

在关闭文件监听的运行实例中，直接修改合成文章后查询无结果；没有手动重建或受管写入，下一次每分钟对账后结果出现，证明对账不依赖监听。

## 离线与交付检查

生产入口与 **65 个本地构建资源**用不使用缓存的请求全部返回成功，响应为 `no-store`。CSP 的脚本与连接范围限定为本地；Mermaid、PDF.js 及其 worker 已在浏览器中实际加载。日常脚本没有依赖安装、升级或外部请求。本次没有断开用户整机网络或清除用户其他站点数据；此处记录的是无缓存、本地资源与外联限制验证。

公开提交只包含程序、五类通用模板、维护说明与合成测试。正式知识、证据快照、附件、私人根映射、浏览器草稿、构建产物、缓存、日志及截图不进入提交。正式库单独核验格式和关系，原有历史测试日期、环境与结果保持历史身份。

## 验证边界

强杀进程、磁盘错误注入和 Windows 文件锁不等同于断电或介质损坏保证。绕过协议直接写文件只能记录实际扫描到的状态。浏览器扩展曾对回环地址显示 `ERR_BLOCKED_BY_CLIENT`；页面验收使用应用内浏览器，未更改扩展或系统保护设置。构建仅出现依赖包的注释位置提示，未阻止构建。

## 启动器身份文件回归（2026-09-21）

针对“实例身份文件缺失”的报告补充核验。现场重试时身份文件与运行实例一致，原报错未再次出现，因此无法确定原报错的具体触发原因。代码存在端口开始响应早于身份文件落盘的窗口；启动器现在对缺失的实例文件最多等待 3 秒，之后仍执行完整身份核验。其他读取与解析错误立即给出文件路径及错误代码，不输出文件内容。

`node tests/runtime-check.ts`：**6 组通过**，使用隔离状态目录与合成库。新增模拟端口已响应、身份文件延迟 500 ms 写入的场景；验证真正缺失的身份在启动等待结束和停止时均被拒绝，既有实例保持运行且未知状态目录未被创建；格式损坏返回 `INVALID_JSON` 且不泄漏文件内容。原有端口占用、陈旧身份、重复启动、错误库、停止与 65 个生产资源检查继续通过。

`tsc --noEmit` 与 `pnpm roadmap:validate` 通过。测试结束后通过仓库根目录 `启动网页.cmd` 恢复原本机实例并调用默认浏览器，首页 HTTP 200；此前已验证重复启动复用同一实例。此轮没有重新执行完整存储测试或 Unity 测试。
